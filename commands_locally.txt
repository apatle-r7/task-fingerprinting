cd /Users/apatle/fingerprinting

python3 -m venv venv
source venv/bin/activate

pip install -r requirements.txt

python3 -m fingerprint.cli --mode local --output fingerprint_report.json

cat fingerprint_report.json

